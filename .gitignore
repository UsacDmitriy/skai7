# Data & generated output
output/
data/skai.duckdb

# Environment (local secrets — шаблон .env.example коммитится)
.env

# AI-кэш (§8.1): placeholder Волны 3 + реальные значения b16/b17 — коммитим в git
!data/ai/*.json

# Datasets: track lightweight data, exclude only heavy binary media
datasets/media/
datasets/**/*.mp4
datasets/**/*.mov
datasets/**/*.mkv

# Frontend (web/)
web/node_modules/
web/dist/
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.mypy_cache/
.pytest_cache/
**/.ipynb_checkpoints/

# JUnit-отчёты для gen_status.py (t5 · §8.9): пишет scripts/check.sh / CI, не в VCS
reports/

# Тест-покрытие (артефакты pytest-cov / vitest — не в VCS, гейт x5)
.coverage
.coverage.*
htmlcov/
coverage/
web/coverage/

# Tooling / worktrees
.kilo/
.playwright-mcp/
skai7_clean/

# Assets / heavy media
05_video_telematics_single_window/
*.png

# OS / logs
**/.DS_Store
.DS_Store
*.log

# git worktrees (вложенные, не трекаются)
.worktrees/

# OpenRouter MCP secrets
tools/openrouter-mcp/.env

# ClinePass MCP secrets
tools/clinepass-mcp/.env
# Keep a pre-migration local key ignored until the owner removes or relocates it.
tools/cline-mcp/.env

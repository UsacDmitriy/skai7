# Корневой requirements — зависимости бэкенда живут в api/requirements.txt
# (fastapi / uvicorn / duckdb / pydantic / pandas — см. там).
-r api/requirements.txt

# TODO(NLU): faster-whisper вернётся для голосового NLU (трек B), удалён вместе со Streamlit-прототипом.

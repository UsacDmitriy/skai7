# Local connection settings. Copy to .env; .env is ignored by git.
# Never add model slugs here: they belong in the committed models.env registry.
CLINE_API_KEY=...
CLINE_API_URL=https://api.cline.bot/api/v1/chat/completions
CLINE_TIMEOUT_SECONDS=600

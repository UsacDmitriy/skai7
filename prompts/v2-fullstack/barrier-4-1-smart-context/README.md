# Барьер 4.1 · smoke умное событие + прогнозы

Промежуточный барьер после Волны 4.1. Выполняется в основном окне `skai_7` на ветке `integration`.
`main` **не трогает** (продвинется на финальном барьере x7). Содержит GUARD чистоты worktree.

- `x6-smoke-context-forecast.md` — merge `feat/*` (с GUARD) → smoke предрасчёта (`incident_scene`/
  `incident_weather`), эндпоинтов `/forecast`/`/zones`/`/fatigue`, обогащённого `risk_score`, фикстур.

Схема — [`../EXECUTION.md`](../EXECUTION.md).

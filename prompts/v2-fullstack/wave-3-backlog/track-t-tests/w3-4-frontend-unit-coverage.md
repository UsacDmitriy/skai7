# W3-4 · Frontend unit-покрытие всего решения (дозакрытие t3)

> Волна 3 · хардненинг. Track **T** (Claude Code, `feat/tests`). Против `00-CONTRACT.md` §3.1/§4/§7.5.
> **Исполнение:** bounded ClinePass package; role `worker`; route category `code`; exact route/model only from `tools/clinepass-mcp/models.env` — детерминированная логика/вёрстка против контракта; гейт = секция Check.
> **Владеет:** `web/src/**/*.test.tsx`, `web/src/test/**` (расширение). **Дополняет, а не дублирует**
> `track-t-tests/t3` (d2/f2/f4 уже покрыты). **НЕ редактирует продуктовые компоненты** — баг → дефект
> треку D/F. Гейт покрытия проверяется на Барьере 3 (x5). Стек тот же: vitest + RTL + jsdom (из t3).

## Цель

Довести компонентное/unit-покрытие фронта до **всех экранов и примитивов по промптам `d3–d5`, `f5–f13`** —
быстро (jsdom), без бэкенда, на фикстурах (`VITE_USE_FIXTURES=true`).

## Состав — по одному `*.test.tsx` на непокрытый модуль

`web/src/components/**/*.test.tsx`:
- `d3` component-lib: композиции (`Card`/`DataTable`/тулбары) рендерятся и пробрасывают props.
- `d4` map-primitives: маркер красится по `severity`; кластеризация/дедуп «1 `unit_id` = 1 маркер» (расширить t3).
- `d5` voice-timeline: таймлайн принимает `TelemetryPoint[]`+события, `playheadOffset` двигает курсор; «🎤»-состояния.

`web/src/pages/**/*.test.tsx`:
- `f5` events-feed: фильтр «Нет видео», поиск, ролевой switcher, клик строки → переход на карточку.
- `f6` monitor-map: ролевой режим «Логист» скрывает DMS-слой; дедуп маркеров.
- `f7` analytics-voice: 🎤→`transcribe`→`query`→`ConfirmationModal`→дашборд В-1/В-2 (toggle По водителям/По ТС);
  клик нарушения → видео-панель (DMS→ch5, ADAS→ch1).
- `f8` tickets: рендер `Ticket[]`; **оверлей «⏱ Просрочено» по `is_overdue`, а не по статусу** (W3-1); enum `Status` отображается.
- `f9` dispatch-alert: карточка `DispatchAlert` с видео-окном ±15с.
- `f10` trip-dossier: трек + таймлайн; `has_video` управляет иконкой.
- `f11` reb-recovery: `gap_periods[]` визуализируются; пустой список → «разрывов нет».
- `f12` sabotage: виджет событий саботажа на фикстуре.
- `f13` role-toggle: переключение роли меняет видимость слоёв/колонок согласованно во всех экранах.

`web/src/pages/**/*.states.test.tsx` (состояния и паритет — перенесено из аудита барьеров):
- У **каждого** экрана `f5–f13`: **loading** (скелетон/спиннер), **empty** (пустой набор → дружелюбная плашка,
  не белый экран), **error** (сбой запроса → плашка «Повторить»). Мокать клиент на reject/`[]`/pending.
- **Паритет live↔fixtures:** компонент рендерит ту же форму на фикстуре (`VITE_USE_FIXTURES=true`) — типы §3.1/§7.5
  совпадают; отсутствие поля не роняет рендер.

## Check

- `cd web && npx vitest run` зелёный целиком — **полный регресс** фронта (не только новые файлы); `npm run typecheck` без ошибок.
- Покрытие `web/src` ≥ **80%** (`npx vitest run --coverage`).
- У каждого экрана `f5–f13` есть тест на **loading/empty/error** (нет необработанного состояния).
- Тест `f8` доказывает: «Просрочено» — оверлей по `is_overdue`, отдельный от `status`; `f6`/`d4` — дедуп «1 `unit_id` = 1 маркер».

## Коммит (обязательно)

Заверши промпт коммитом в свою ветку — **merge на барьере берёт только коммиты**; незакоммиченная
работа в worktree на барьер не попадёт:

```bash
git add -A && git commit -m "w3-4: <что сделано>"
```

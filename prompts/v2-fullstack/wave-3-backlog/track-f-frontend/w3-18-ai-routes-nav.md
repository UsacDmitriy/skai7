# W3-18 · AI-маршруты + меню: `/copilot`, `/metrics` (подготовка Волны 4, против экранов-сирот)

> Волна 3 · бэклог (**подготовка под Волну 4**). Трек **Frontend** (владелец f1 — роутинг/оболочка).
> Против `00-CONTRACT.md` §8.3/§8.7. **Исполнение:** bounded ClinePass package; role `worker`; route category `code`; exact route/model only from `tools/clinepass-mcp/models.env` — маршрутизация/вёрстка; гейт = typecheck.
> **Владеет:** `web/src/App.tsx` (маршруты + NAV). Зависит от `w3-13` (`ComingSoon`) и `w3-17` (типы/клиент).
> Разблокирует **f17** (копилот) и **f21** (`/metrics`) — даёт им маршрут + пункт меню, чтобы не были сиротами.

## Контекст (целостность)

f17 (копилот) и f21 (`/metrics`) — новые экраны Волны 4. Без заранее заведённых маршрутов и пунктов меню они
повторят проблему экранов-сирот (как `/trip`/`/reb` до Волны 3). Заводим маршрут+меню сейчас, экран — до Волны 4
показывает `ComingSoon` (из `w3-13`), затем f17/f21 **заменяют** содержимое страницы.

## Что сделать

1. **Маршруты** в `web/src/App.tsx` (внутри `AppShell`, рядом с `/fleet-health` из `w3-13`):
   - `/copilot` → lazy `Copilot` (до Волны 4 — `ComingSoon` «AI-копилот · Волна 4.2»);
   - `/metrics` → lazy `Metrics` (до Волны 4 — `ComingSoon` «Метрики и качество данных · Волна 4.3»).
   Реальные страницы создают f17/f21 — этот промпт лишь регистрирует маршрут и временный таргет.
2. **Группа меню «AI»** в `NAV`: пункты «Копилот» (`/copilot`), «Метрики» (`/metrics`) с бейджем `W4`
   (честно «скоро», как сигнпостинг `w3-13`). Бейдж снимается, когда f17/f21 влиты (в их промптах).
3. Не плодить лишние пункты: scene/forecast/heatmap/waterfall/verdict (f15/f16/f18/f19/f20) — **аддитивны**
   к существующим экранам (карточка/отчёт/монитор/саботаж), отдельные маршруты им не нужны.

## Check

- `/copilot` и `/metrics` рендерят `ComingSoon` с описанием (не generic 404); маршруты в `App.tsx` присутствуют.
- В меню есть группа «AI» с пунктами «Копилот»/«Метрики» (бейдж `W4`); `aria-current` работает.
- `npm run typecheck` зелёный; lazy+Suspense как у остальных маршрутов.
- f17/f21 (Волна 4) смогут заменить таргет маршрута без правок роутинга (точка расширения готова).

## Коммит (обязательно)

```bash
git add -A && git commit -m "w3-18: AI-маршруты /copilot,/metrics + меню (каркас под f17/f21)"
```

# Барьер 4.2 · e2e AI-слой (ассистент)

Барьер Волны 4.2. Выполняется в основном окне `skai_7` на ветке `integration`.
Содержит GUARD чистоты worktree; при зелёном e2e оставляет ассистента на `integration`.

> **`main` НЕ трогаем.** x7 не продвигает `main` (как и x6). Финальный ff-merge AI-слоя в
> `main` делает барьер **x8** Волны 4.3 — после ops & trust + полного регресса + live-smoke.

- `x7-e2e-wave4.md` — merge `feat/*` (с GUARD) → полный e2e AI-слоя (умное событие→карточка,
  прогноз→отчёт, копилот, heatmap/зоны, вердикт саботажа) + регресс. Зелёный → переход к Волне 4.3.

Схема — [`../EXECUTION.md`](../EXECUTION.md).

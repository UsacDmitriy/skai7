# Барьер 4.2 · e2e AI-слой → main

Финальный барьер Волны 4. Выполняется в основном окне `skai_7` на ветке `integration`.
Содержит GUARD чистоты worktree; при зелёном e2e **продвигает `main`** (ff-only).

- `x7-e2e-wave4.md` — merge `feat/*` (с GUARD) → полный e2e AI-слоя (умное событие→карточка,
  прогноз→отчёт, копилот, heatmap/зоны, вердикт саботажа) + регресс → `main`.

Схема — [`../EXECUTION.md`](../EXECUTION.md).
